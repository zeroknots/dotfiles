config:
  fallback-icon: "?" # icon to use if no definition is found
  show-name: true # show the window name with the icon

icons:
  nvim: ""
  fish: "" # overwrite with your own symbol (Nerd Font icon, emoji, whatever!)
